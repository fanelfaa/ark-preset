import { Switch, SwitchLabel, Tooltip, TooltipTrigger, TooltipContent } from "@fan-ui/solid";

# solid-marked Demo

This page is rendered **entirely** from a `.md` file via `vite-plugin-solid-marked`.

## Features

- **GFM tables** like the one below
- `inline code` with proper styling
- [External link](https://example.com)
- Code blocks with syntax highlighting

## Markdown features

> Blockquotes with **bold** and `inline code`.

### Nested lists

1. First item
   - Nested unordered
   - More nested
2. Second item
   - Another nested
3. Third item

## Code block

```ts
function greet(name: string): string {
  return `Hello, ${name}!`;
}
```

## Table

| Component | Status | Version |
|-----------|--------|---------|
| Button | ✅ Done | 0.1.0 |
| Input | ✅ Done | 0.1.0 |
| Dialog | 🚧 WIP | 0.2.0 |

## Horizontal rule

---

## Embedded components

<div class="rounded-lg border border-border p-6 not-prose">
  <div class="flex flex-col gap-4">
    <Switch>
      <SwitchLabel>Off</SwitchLabel>
    </Switch>
    <Switch defaultChecked>
      <SwitchLabel>On</SwitchLabel>
    </Switch>
    <Switch disabled>
      <SwitchLabel>Disabled</SwitchLabel>
    </Switch>
  </div>
</div>

<Tooltip>
  <TooltipTrigger>With Arrow</TooltipTrigger>
  <TooltipContent useArrow>Tooltip with arrow</TooltipContent>
</Tooltip>

This content comes from `solid-marked-demo.md` compiled at **build time**.
