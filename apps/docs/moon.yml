language: typescript
layer: application
stack: frontend
dependsOn:
  - solid

tasks:
  dev:
    command: "pnpm dev"
  build:
    command: "pnpm build"
    inputs:
      - "src/**/*"
      - "astro.config.mjs"
      - "package.json"
    outputs:
      - "dist"
  preview:
    command: "pnpm preview"
    deps:
      - "build"
