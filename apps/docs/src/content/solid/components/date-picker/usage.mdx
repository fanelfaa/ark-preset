import ComponentPreview from '../../../../components/ComponentPreview.astro';
import DatePickerBasicDemo from '@demos/date-picker-demo/DatePickerBasicDemo.tsx';
import DatePickerBasicDemoCode from '@demos/date-picker-demo/DatePickerBasicDemo.tsx?code';
import DatePickerRangeDemo from '@demos/date-picker-demo/DatePickerRangeDemo.tsx';
import DatePickerRangeDemoCode from '@demos/date-picker-demo/DatePickerRangeDemo.tsx?code';
import DatePickerMultipleDemo from '@demos/date-picker-demo/DatePickerMultipleDemo.tsx';
import DatePickerMultipleDemoCode from '@demos/date-picker-demo/DatePickerMultipleDemo.tsx?code';
import DatePickerControlledDemo from '@demos/date-picker-demo/DatePickerControlledDemo.tsx';
import DatePickerControlledDemoCode from '@demos/date-picker-demo/DatePickerControlledDemo.tsx?code';

## Usage


### Basic Usage

<ComponentPreview code={DatePickerBasicDemoCode}>
  <DatePickerBasicDemo client:load />
</ComponentPreview>

### Selection Modes

The date picker supports three selection modes via the `selectionMode` prop:

#### Single (default)

Select one date at a time. This is the default mode.

<ComponentPreview code={DatePickerBasicDemoCode}>
  <DatePickerBasicDemo client:load />
</ComponentPreview>

#### Range

Select a start and end date. The control renders two inputs — one for each end of the range.

<ComponentPreview code={DatePickerRangeDemoCode}>
  <DatePickerRangeDemo client:load />
</ComponentPreview>

#### Multiple

Select multiple individual dates. The control renders selected dates as removable chips.

<ComponentPreview code={DatePickerMultipleDemoCode}>
  <DatePickerMultipleDemo client:load />
</ComponentPreview>

Use `maxSelectedDates` to limit the number of selections in multiple mode:

```tsx
<DatePicker
  selectionMode="multiple"
  maxSelectedDates={3}
  label="Up to 3 dates"
/>
```

> **Note:** `closeOnSelect` is automatically ignored when `selectionMode` is `"multiple"` (Ark UI behavior).

### Controlled

Control the selected date programmatically using `value` and `onValueChange`.

<ComponentPreview code={DatePickerControlledDemoCode}>
  <DatePickerControlledDemo client:load />
</ComponentPreview>
