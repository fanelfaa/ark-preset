import ComponentPreview from '../../../../components/ComponentPreview.astro';
import ListboxBasicDemo from '@demos/listbox-demo/ListboxBasicDemo.tsx';
import ListboxBasicDemoCode from '@demos/listbox-demo/ListboxBasicDemo.tsx?code';
import ListboxMultipleDemo from '@demos/listbox-demo/ListboxMultipleDemo.tsx';
import ListboxMultipleDemoCode from '@demos/listbox-demo/ListboxMultipleDemo.tsx?code';
import ListboxHorizontalDemo from '@demos/listbox-demo/ListboxHorizontalDemo.tsx';
import ListboxHorizontalDemoCode from '@demos/listbox-demo/ListboxHorizontalDemo.tsx?code';
import ListboxRootProviderDemo from '@demos/listbox-demo/ListboxRootProviderDemo.tsx';
import ListboxRootProviderDemoCode from '@demos/listbox-demo/ListboxRootProviderDemo.tsx?code';

## Usage


### Basic Usage

A basic listbox for single-item selection.

<ComponentPreview code={ListboxBasicDemoCode}>
  <ListboxBasicDemo client:load />
</ComponentPreview>

### Multiple Selection

Use `selectionMode="multiple"` to allow selecting multiple items:

<ComponentPreview code={ListboxMultipleDemoCode}>
  <ListboxMultipleDemo client:load />
</ComponentPreview>

### Horizontal Orientation

Use `orientation="horizontal"` for a horizontal layout:

<ComponentPreview code={ListboxHorizontalDemoCode}>
  <ListboxHorizontalDemo client:load />
</ComponentPreview>

## Advanced Usage

When the composite `Listbox` doesn't provide enough control, import the raw primitive parts from the base file directly:

```tsx
import { Listbox } from "~/components/listbox/listbox.base";
```

Or import `ListboxBase` (the raw parts namespace) from the composite entry point:

```tsx
import { ListboxBase } from "~/components/listbox";
```

### RootProvider Pattern

For full control over the listbox machine, use `useListbox` with `ListboxBase.RootProvider`:

<ComponentPreview code={ListboxRootProviderDemoCode}>
  <ListboxRootProviderDemo client:load />
</ComponentPreview>
