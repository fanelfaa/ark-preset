import { DatePicker } from "@ark-preset/solid";

export default function DatePickerMultipleDemo() {
  return (
    <DatePicker selectionMode="multiple" label="Multiple dates" placeholder="Select dates..." />
  );
}
