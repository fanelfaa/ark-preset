import { Button, Spinner, buttonVariants } from "@ark-preset/solid";
import { DemoWrapper } from "../../../components/DemoWrapper";

## Usage

Import the component:

```tsx
import { Button } from "~/components/button";
```

Basic usage:

```tsx
<Button>Click me</Button>
```

<DemoWrapper class="flex flex-wrap gap-4 not-prose">
  <Button>Click me</Button>
</DemoWrapper>

### Variants

Use the <code>variant</code> prop to change the visual style.

<DemoWrapper class="flex flex-wrap gap-4 not-prose">
  <Button>Default</Button>
  <Button variant="secondary">Secondary</Button>
  <Button variant="destructive">Destructive</Button>
  <Button variant="outline">Outline</Button>
  <Button variant="ghost">Ghost</Button>
  <Button variant="link">Link</Button>
</DemoWrapper>

### Sizes

Use the <code>size</code> prop to change the button size.

<DemoWrapper class="flex flex-wrap items-center gap-4 not-prose">
  <Button size="xs">Xs</Button>
  <Button size="sm">Sm</Button>
  <Button size="md">Md</Button>
  <Button size="lg">Lg</Button>
  <Button size="icon-xs">
    <svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
      <path d="M5 12h14" />
      <path d="M12 5v14" />
    </svg>
  </Button>
  <Button size="icon-sm">
    <svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
      <path d="M5 12h14" />
      <path d="M12 5v14" />
    </svg>
  </Button>
  <Button size="icon-md">
    <svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
      <path d="M5 12h14" />
      <path d="M12 5v14" />
    </svg>
  </Button>
  <Button size="icon-lg">
    <svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
      <path d="M5 12h14" />
      <path d="M12 5v14" />
    </svg>
  </Button>
</DemoWrapper>

### Disabled

Add the <code>disabled</code> prop to disable interaction.

<DemoWrapper class="flex flex-wrap gap-4 not-prose">
  <Button disabled>Default</Button>
  <Button variant="secondary" disabled>Secondary</Button>
  <Button variant="destructive" disabled>Destructive</Button>
  <Button variant="outline" disabled>Outline</Button>
  <Button variant="ghost" disabled>Ghost</Button>
  <Button variant="link" disabled>Link</Button>
</DemoWrapper>

### With Icon

```tsx
{/* Icon button */}
<Button variant="outline" size="icon">
  <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
    <path d="M5 12h14" />
    <path d="M12 5v14" />
  </svg>
</Button>

{/* Icon at start */}
<Button variant="outline">
  <svg data-icon="inline-start" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
    <path d="M5 12h14" />
    <path d="M12 5v14" />
  </svg>
  New Branch
</Button>

{/* Icon at end */}
<Button variant="outline">
  Fork
  <svg data-icon="inline-end" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
    <path d="M5 12h14" />
    <path d="M12 5v14" />
  </svg>
</Button>
```

<DemoWrapper class="flex flex-wrap items-center gap-4 not-prose">
  <Button variant="outline" size="icon">
    <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
      <path d="M5 12h14" />
      <path d="M12 5v14" />
    </svg>
  </Button>
  <Button variant="outline">
    <svg data-icon="inline-start" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
      <path d="M5 12h14" />
      <path d="M12 5v14" />
    </svg>
    New Branch
  </Button>
  <Button variant="outline">
    Fork
    <svg data-icon="inline-end" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
      <path d="M5 12h14" />
      <path d="M12 5v14" />
    </svg>
  </Button>
</DemoWrapper>

### Spinner

```tsx
{/* Icon button with spinner */}
<Button disabled variant="outline" size="icon">
  <Spinner />
</Button>

{/* Spinner at start */}
<Button disabled variant="outline">
  <Spinner data-icon="inline-start" />
  Loading...
</Button>

{/* Spinner at end */}
<Button disabled variant="outline">
  Saving
  <Spinner data-icon="inline-end" />
</Button>
```

<DemoWrapper class="flex flex-wrap items-center gap-4 not-prose">
  <Button disabled variant="outline" size="icon">
    <Spinner />
  </Button>
  <Button disabled variant="outline">
    <Spinner data-icon="inline-start" />
    Loading...
  </Button>
  <Button disabled variant="outline">
    Saving
    <Spinner data-icon="inline-end" />
  </Button>
</DemoWrapper>

### Link

Use the <code>buttonVariants</code> helper to render a link that looks like a button.

```tsx
import { Button, buttonVariants } from "~/components/button";

<a class={buttonVariants({ variant: "outline" })} href="#">
  Click here
</a>;

// or
<Button asChild={(props)=><a {...props()}/>}>
  Click Here
</Button>
```

<DemoWrapper class="flex flex-wrap gap-4 not-prose mb-8">
  <a class={buttonVariants({ variant: "outline" })} href="#">
    Click here
  </a>
</DemoWrapper>
