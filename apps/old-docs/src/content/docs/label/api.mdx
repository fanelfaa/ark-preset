## API Reference

The `labelVariants` function from `@ark-preset/core` accepts the following options:

| Option | Type | Default | Description |
|--------|------|---------|-------------|
| class | `string` | — | Additional CSS classes to merge |
| error | `boolean` | `false` | Applies `text-destructive` for error state |
