import { InlineCode } from "@ark-preset/solid";
import { ComponentPreview } from "../../../components/ComponentPreview";
import SelectRootProviderDemo from "@demos/select-demo/SelectRootProviderDemo.tsx";
import SelectRootProviderDemoCode from "@demos/select-demo/SelectRootProviderDemo.tsx?code";
import SelectMultipleDemo from "@demos/select-demo/SelectMultipleDemo.tsx";
import SelectMultipleDemoCode from "@demos/select-demo/SelectMultipleDemo.tsx?code";
import SelectSearchableDemo from "@demos/select-demo/SelectSearchableDemo.tsx";
import SelectSearchableDemoCode from "@demos/select-demo/SelectSearchableDemo.tsx?code";

## Usage

Import the components:

```tsx
import { createListCollection } from "@ark-ui/solid";
import { Index } from "solid-js";
import {
  Select,
  SelectLabel,
  SelectTrigger,
  SelectContent,
  SelectItem,
} from "~/components/select";
```

Basic usage:

```tsx
const items = createListCollection({
  items: [
    { label: 'React', value: 'react' },
    { label: 'Solid.js', value: 'solid' },
  ],
})

<Select collection={items}>
  <SelectLabel>Framework</SelectLabel>
  <SelectTrigger placeholder="Select..." />
  <SelectContent>
    <Index each={items.items}>
      {(item) => <SelectItem item={item()}>{item().label}</SelectItem>}
    </Index>
  </SelectContent>
</Select>
```

### With Label

Add a label to describe the select field.

```tsx
<Select collection={items}>
  <SelectLabel>Choose your framework</SelectLabel>
  <SelectTrigger placeholder="Select..." />
  <SelectContent>
    <Index each={items.items}>
      {(item) => <SelectItem item={item()}>{item().label}</SelectItem>}
    </Index>
  </SelectContent>
</Select>
```

### Root Provider

Use <InlineCode>SelectRootProvider</InlineCode> when you need to access the select state outside of the component tree. This pattern uses the <InlineCode>useSelect</InlineCode> hook from Ark UI to create a shared context that both the select and external elements can reference.

<ComponentPreview code={SelectRootProviderDemoCode}>
  <SelectRootProviderDemo />
</ComponentPreview>

The key difference:

- **<InlineCode>Select</InlineCode>** — manages its own state internally. Use for simple, self-contained selects.
- **<InlineCode>SelectRootProvider</InlineCode>** — accepts a pre-created select context via <InlineCode>useSelect</InlineCode>. Use when you need to read or control the select state from outside the component tree.

### Multiple Selection

Use the <InlineCode>multiple</InlineCode> prop to allow selecting multiple options. The <InlineCode>SelectTrigger</InlineCode> composite includes a clear button to deselect all selections.

<ComponentPreview code={SelectMultipleDemoCode}>
  <SelectMultipleDemo />
</ComponentPreview>

### Searchable Selection

Use the <InlineCode>searchable</InlineCode> prop to enable a search input inside the dropdown. Pass <InlineCode>onSearch</InlineCode> to filter the collection as the user types.

<ComponentPreview code={SelectSearchableDemoCode}>
  <SelectSearchableDemo />
</ComponentPreview>

The <InlineCode>searchable</InlineCode> prop works with both single and <InlineCode>multiple</InlineCode> selection modes.

### Error State

Use the <InlineCode>error</InlineCode> prop on <InlineCode>SelectBase.Root</InlineCode> to show an error state. The structured <InlineCode>Select</InlineCode> component delegates its root styling to <InlineCode>SelectBase.Root</InlineCode>, so the <InlineCode>error</InlineCode> prop requires using the base component directly:

```tsx
<SelectBase.Root collection={items} error>
  <SelectLabel>Framework</SelectLabel>
  <SelectTrigger placeholder="Select..." />
  <SelectContent>
    <Index each={items.items}>
      {(item) => <SelectItem item={item()}>{item().label}</SelectItem>}
    </Index>
  </SelectContent>
</SelectBase.Root>
```

### Composite Exports

The <InlineCode>SelectTrigger</InlineCode>, <InlineCode>SelectContent</InlineCode>, and <InlineCode>SelectItem</InlineCode> components are composite wrappers from the barrel entry point that include inline SVG icons and Portal. <InlineCode>SelectTrigger</InlineCode> also includes a clear button when used with <InlineCode>multiple</InlineCode> selection. Import them alongside <InlineCode>Select</InlineCode>:

```tsx
import { Select, SelectLabel, SelectTrigger, SelectContent, SelectItem } from "~/components/select";
```

## Advanced Usage

When you need more control, import raw primitive parts directly from the base file:

```tsx
import { Select } from "~/components/select/select.base";
```

Or import <InlineCode>SelectBase</InlineCode> (the raw parts namespace) from the barrel entry point:

```tsx
import { SelectBase } from "~/components/select";
```
