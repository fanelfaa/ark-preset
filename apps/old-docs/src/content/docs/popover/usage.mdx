import { InlineCode } from "@ark-preset/solid";
import { ComponentPreview } from "../../../components/ComponentPreview";
import PopoverRootProviderDemo from "@demos/popover-demo/PopoverRootProviderDemo.tsx";
import PopoverRootProviderDemoCode from "@demos/popover-demo/PopoverRootProviderDemo.tsx?code";

## Usage

Import the components:

```tsx
import {
  Popover,
  PopoverTrigger,
  PopoverContent,
  PopoverTitle,
  PopoverDescription,
} from "~/components/popover";
```

Basic usage:

```tsx
<Popover>
  <PopoverTrigger>Open Popover</PopoverTrigger>
  <PopoverContent>
    <PopoverTitle>Title</PopoverTitle>
    <PopoverDescription>Description text</PopoverDescription>
  </PopoverContent>
</Popover>
```

> **Note:** <InlineCode>PopoverArrow</InlineCode> and <InlineCode>PopoverCloseTrigger</InlineCode> are automatically included inside <InlineCode>PopoverContent</InlineCode>. You only need to import them separately for custom positioning or advanced use cases.

## Root Provider

Use <InlineCode>PopoverRootProvider</InlineCode> when you need to access the popover state outside of the component tree. This pattern uses the <InlineCode>usePopover</InlineCode> hook from Ark UI to create a shared context that both the popover and external elements can reference.

<ComponentPreview code={PopoverRootProviderDemoCode}>
  <PopoverRootProviderDemo />
</ComponentPreview>

The key difference:

- **<InlineCode>Popover</InlineCode>** — manages its own state internally. Use for simple, self-contained popovers.
- **<InlineCode>PopoverBase.RootProvider</InlineCode>** — accepts a pre-created popover context via <InlineCode>usePopover</InlineCode>. Use when you need to read or control the popover state from outside the component tree.
