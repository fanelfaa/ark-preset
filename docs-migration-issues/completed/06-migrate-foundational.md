## What to build

Migrate the Homepage, Installation/Setup pages, and exactly _one_ complex component (e.g., `Button`) into the `solid` collection. Adapt its MDX to use the new `ComponentPreview.astro`. This acts as a tracer bullet to prove the entire new pipeline works on real content before bulk migrating.

## Acceptance criteria

- [ ] Homepage and Setup pages are migrated and fully functional.
- [ ] The Button component (or equivalent complex component) is migrated to the `solid` collection.
- [ ] The Button's MDX successfully uses `ComponentPreview.astro` for its live demos.
- [ ] E2E tests navigate through the Homepage, Setup, and the Button page successfully.

## Blocked by

- Issue 05 (05-component-preview.md)
